import { Box, Container, Typography } from '@mui/material';

function Header() {
  return (
    <Box width='100%'>
        <Typography variant="h2" align="center" sx={{color: 'white'}}>Infinite Chess</Typography>
    </Box>
  );
}

export default Header;